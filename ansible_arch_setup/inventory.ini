[archlinux]
# Target localhost for workstation setup
# ansible_connection=local tells Ansible to run directly on the machine
# ansible_python_interpreter ensures Ansible uses the correct Python
localhost ansible_connection=local ansible_python_interpreter=/usr/bin/python3
