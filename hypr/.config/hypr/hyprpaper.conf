
# Hyprpaper configuration for random wallpaper rotation

splash = on                    # Enable smooth splash animation
reload_interval = 60          
order = random
recursive = true

wallpaper {
    monitor = *                # Apply to all monitors (wildcard)
        path = $HOME/Pictures/WallPappers/
    }
